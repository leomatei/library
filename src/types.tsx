export interface Book {
  id: string
  title: string
  author: string
  description: string
}
export interface BookDOM {
  bookId: string
  title: string
  author: string
  description: string
}

export interface FormData {
  title: string
  author: string
  description: string
}
